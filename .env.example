ANTHROPIC_API_KEY=

POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Required by the Exam Practice flow. Set automatically when you provision a
# Vercel Blob store for the Sentiero project (Storage -> Create -> Blob).
# Exam uploads go direct to Blob from the browser; the server fetches each
# blob, extracts the text, persists it, then deletes the blob.
BLOB_READ_WRITE_TOKEN=
